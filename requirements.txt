# Dependencies of the EWF Tech Simulator (Python 3.9, as embedded in Rhino 8).
#
# Inside Rhino/Grasshopper these are installed automatically from the
# '# requirements:' line at the top of each script; keep that line identical
# to this file. This file is for development outside Rhino (tests, linting).
numpy==1.24.4
pandas==1.5.3
pytz==2025.2
astral==3.2
scipy==1.13.1
openpyxl==3.1.5
